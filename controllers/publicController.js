const STUDENT_MODEL = require("../models/studentModel")

const findrole = async(req, res)=>{
    try {
        const user = STUDENT_MODEL.find()
    } catch (error) {
        
    }
}

module.exports = findrole 